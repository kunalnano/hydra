/**
 * Auto-updater stub for HELM.
 *
 * electron-updater is not currently installed. This module exports a no-op
 * placeholder so the import path exists when we're ready to wire it up.
 *
 * To enable real auto-updates:
 * 1. npm install electron-updater
 * 2. Replace this stub with the real implementation
 * 3. Call checkForUpdates() from main/index.ts after app.whenReady()
 */

export function checkForUpdates(): void {
  console.log('[updater] Auto-update checking is not yet configured.')
}
